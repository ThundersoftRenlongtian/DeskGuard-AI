# DeskGuard AI — Demo 开发方案

## 目标

用 PC 摄像头 + Python + AI模型 API，跑通核心功能链路，证明方案可行。

---

## Demo 功能范围

| 功能 | 实现方式 | 优先级 |
|------|----------|--------|
| 实时坐姿检测 | PC摄像头 + MediaPipe Pose | P0 |
| 坐姿分类 | 基于关键点角度的规则判断（驼背/前倾/正常） | P0 |
| AI 智能提醒 | 触发条件 → 调 AI API → 生成提醒文案 | P0 |
| 桌面弹窗通知 | Windows 系统通知 (plyer/win10toast) | P0 |
| 久坐计时 | 计时器 + 状态机 | P1 |
| 数据记录 | SQLite 本地存储 | P1 |
| AI 日报生成 | 调 AI API 生成自然语言报告 | P2 |
| LED灯光反馈 | Arduino/ESP32 + USB串口 (选做) | P3 |

---

## 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| 姿态检测 | MediaPipe Pose (Python) | 免费、精度高、33关键点、实时 |
| AI模型 | MiMo / DeepSeek / 通义千问 | 根据你的选择 |
| 桌面通知 | plyer 或 win10toast | 跨平台/Windows原生 |
| 数据存储 | SQLite | 零配置、本地 |
| UI界面 | OpenCV窗口 (简单) 或 Tkinter | Demo够用 |

---

## 架构图

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  PC 摄像头    │ ──▶ │ MediaPipe    │ ──▶ │ 坐姿分类     │
│  (cv2)       │     │ Pose 33点    │     │ (角度规则)   │
└──────────────┘     └──────────────┘     └──────────────┘
                                                │
                                                ▼
                                     ┌──────────────────┐
                                     │ 状态机 (计时/触发) │
                                     │ 驼背>60s? 久坐>45m? │
                                     └──────────────────┘
                                                │ 触发
                                                ▼
                                     ┌──────────────────┐
                                     │ AI API 调用       │
                                     │ (生成提醒内容)    │
                                     └──────────────────┘
                                                │
                                                ▼
                                     ┌──────────────────┐
                                     │ 输出              │
                                     │ • 桌面弹窗通知    │
                                     │ • 终端日志        │
                                     │ • (可选) LED灯    │
                                     └──────────────────┘
```

---

## 坐姿分类逻辑

利用 MediaPipe 33个关键点中的关键部位：

```python
# 关键角度计算
肩部前倾角 = 计算(耳朵, 肩膀, 髋部) 的角度
头部下垂角 = 计算(鼻子到肩膀中点) 的垂直偏移

# 分类规则
if 肩部前倾角 < 150°:  → 驼背
if 头部下垂角 > 阈值:  → 低头
if 左右肩高度差 > 阈值: → 歪坐
else: → 正常坐姿
```

---

## AI 提醒 Prompt 设计

```
系统提示：
你是一个桌面健康助手。用户的坐姿传感器检测到了问题，
请根据以下数据生成一条简短、温和、有用的提醒消息（30字以内）。

用户输入：
- 当前问题：驼背
- 持续时间：65秒
- 今日已提醒次数：3
- 当前时间：14:32

请生成提醒消息。
```

---

## 文件结构

```
deskguard-demo/
├── main.py              # 主程序入口
├── pose_detector.py     # MediaPipe 姿态检测
├── posture_classifier.py # 坐姿分类（角度规则）
├── state_machine.py     # 状态机（计时、触发条件）
├── ai_agent.py          # AI API 调用（生成提醒）
├── notifier.py          # 桌面通知
├── data_store.py        # SQLite 数据记录
├── config.py            # 配置（API key、阈值等）
└── requirements.txt     # 依赖
```

---

## 依赖

```
mediapipe
opencv-python
requests
plyer
sqlite3 (内置)
```

---

## 开发步骤

### 第1步：姿态检测跑通（1天）
- 安装 mediapipe + opencv
- 打开摄像头，实时显示骨骼点
- 验证关键点坐标可用

### 第2步：坐姿分类（1天）
- 计算肩部角度、头部偏移
- 实现分类逻辑
- 在画面上显示当前坐姿状态

### 第3步：状态机+触发（0.5天）
- 驼背持续60秒触发提醒
- 久坐45分钟触发提醒
- 冷却机制（同一提醒不重复发）

### 第4步：AI提醒生成（0.5天）
- 接入 AI API
- 发送上下文数据
- 获取自然语言提醒内容

### 第5步：桌面弹窗（0.5天）
- 系统通知弹窗
- 显示AI生成的提醒内容

### 第6步：数据记录+日报（1天）
- SQLite存储每次检测/提醒记录
- 调AI API生成日报

---

## 总计：约 4-5 天可跑通完整 Demo

---

## 待确认

1. AI 模型用哪个？（MiMo 本地部署 / DeepSeek API / 其他）
2. 是否需要 LED 硬件演示？（需要额外买 Arduino + LED）
3. Demo 需要有界面还是终端+弹窗就行？
